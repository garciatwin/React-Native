class UserProfile:
    def __init__(self, gender, weight, height, age, measurements, photo):
        self.gender = gender
        self.weight = weight
        self.height = height
        self.age = age
        self.measurements = measurements
        self.photo = photo

class ProfileCompletionSystem:
    def __init__(self):
        self.profiles = {}

    def complete_profile(self, email, gender, weight, height, age, measurements, photo):
        if email not in self.users:
            return "Email not registered"
        profile = UserProfile(gender, weight, height, age, measurements, photo)
        self.profiles[email] = profile
        return "Profile completion successful"

# Sample usage:
# pcs = ProfileCompletionSystem()
# pcs.complete_profile('our_user@test.com', 'male', 70, 180, 30,
#                      {'chest': 100, 'thighs': 60, 'waist': 80, 'hips': 90, 'arms': 35}, 'path/to/photo.jpg')

